#!/usr/bin/python3
if __name__ == "__main__":
    import sys
    arguments = sys.argv[1:]
    full = 0
    if len(sys.argv) > 0:
        for n in arguments:
            total += int(n)
        print(full)
    else:
        print(full)
